---
name: Bug report
about: Something does not work, or works silently and wrongly
title: ''
labels: ''
assignees: ''
---

## What happened

<!-- What you ran, what you expected, what you got instead. -->

## TouchDesigner build

<!-- e.g. 2025.32460. Help → About inside TouchDesigner, or the first line of
     `td-atlas status`. Write "not installed" if that is the point. -->

## Operating system

<!-- e.g. macOS 15.6 (Apple silicon), Windows 11. Windows is supported blind:
     the paths and separators are handled and tested, but no maintainer has run
     it on a Windows machine, so Windows reports are especially useful. -->

## Output of `td-atlas doctor`

```
<!-- paste the whole output; it walks the chain — environment, TouchDesigner,
     index, index build, probe, bridge, MCP server — and names the broken link -->
```

## Anything else

<!-- The failing tool call and its arguments, the last few lines of
     `td-atlas log --failures`, a project file if it can be shared. -->
